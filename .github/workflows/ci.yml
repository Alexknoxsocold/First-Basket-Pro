name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  runner-smoke:
    name: Runner smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Verify runner starts
        shell: bash
        run: |
          echo "runner-ok"
          uname -a

  build:
    name: Typecheck and production build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Generate non-breaking production audit fixes
        shell: bash
        run: |
          npm install --package-lock-only --ignore-scripts
          npm audit fix --omit=dev --package-lock-only || true

      - name: Verify patched lockfile, audit, typecheck, and production build
        shell: bash
        run: |
          set -o pipefail
          {
            echo "== npm ci =="
            npm ci
            echo "== npm audit --omit=dev =="
            npm audit --omit=dev || true
            echo "== npm run check =="
            npm run check
            echo "== npm run build =="
            npm run build
          } 2>&1 | tee ci-verification.log

      - name: Commit generated lockfile fix
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already clean"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "Apply non-breaking npm audit lockfile fixes"
          git push origin HEAD:${{ github.head_ref }}

      - name: Capture production audit JSON
        if: always()
        shell: bash
        run: npm audit --omit=dev --json > npm-audit-production.json || true

      - name: Upload CI verification artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-verification
          path: |
            ci-verification.log
            npm-audit-production.json
            package-lock.json
          if-no-files-found: warn
          retention-days: 3
