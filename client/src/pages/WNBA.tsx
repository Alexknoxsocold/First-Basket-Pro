import { useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import { RefreshCw, ShieldCheck, Clock, Users, Trophy, AlertCircle, History, CircleDot } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Skeleton } from '@/components/ui/skeleton';

type Candidate={name:string;team:string;position:string;headshot:string|null;avgPoints:number;avgFga:number;fgPct:number;avgMinutes:number;currentFirstBaskets:number;currentGamesTracked:number;previousFirstBaskets:number;previousGamesTracked:number;probability:number;rank:number};
type Game={id:string;date:string;shortName:string;awayTeam:string;homeTeam:string;awayName:string;homeName:string;status:string;lineupStatus:'confirmed'|'waiting';starters:{name:string;team:string}[];candidates:Candidate[];topPick:Candidate|null};
type Slate={season:number;updatedAt:string;teams:{abbreviation:string;name:string}[];games:Game[];source:string;modelVersion:string};
type HistoryRow={playerName:string;team:string;season:number;fbScored:number;gamesTracked:number;rate:number|null;lastUpdated:string};
type HistoryPayload={currentSeason:number;previousSeason:number;current:HistoryRow[];previous:HistoryRow[]};

function time(v:string){const d=new Date(v);return Number.isNaN(d.getTime())?'Time pending':d.toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit',timeZone:'America/New_York'})+' ET'}
function rate(f:number,g:number){return g>0?`${((f/g)*100).toFixed(1)}%`:'—'}
function projectedFor(team:string,history:HistoryRow[]){return history.filter(r=>r.team===team&&r.gamesTracked>0).sort((a,b)=>b.gamesTracked-a.gamesTracked||b.fbScored-a.fbScored).slice(0,5)}

function CandidateRow({p}:{p:Candidate}){return <div className="grid grid-cols-[32px_1fr_auto] sm:grid-cols-[32px_1fr_92px_92px_90px] items-center gap-3 py-3 border-b last:border-b-0"><div className="w-8 h-8 rounded-full overflow-hidden bg-muted flex items-center justify-center text-xs font-bold">{p.headshot?<img src={p.headshot} alt="" className="w-full h-full object-cover object-top"/>:p.rank}</div><div className="min-w-0"><div className="flex items-center gap-2"><span className="font-semibold text-sm truncate">#{p.rank} {p.name}</span><Badge variant="secondary" className="text-[9px]">{p.team}</Badge></div><div className="text-[10px] text-muted-foreground">{p.position} · {p.avgPoints.toFixed(1)} PPG · {p.avgFga.toFixed(1)} FGA</div></div><div className="text-right"><div className="font-mono font-bold text-sm">{p.probability.toFixed(1)}%</div><div className="text-[9px] text-muted-foreground">model</div></div><div className="hidden sm:block text-right"><div className="font-mono text-xs">{p.currentFirstBaskets}/{p.currentGamesTracked}</div><div className="text-[9px] text-muted-foreground">{rate(p.currentFirstBaskets,p.currentGamesTracked)} this yr</div></div><div className="hidden sm:block text-right"><div className="font-mono text-xs">{p.previousFirstBaskets}/{p.previousGamesTracked}</div><div className="text-[9px] text-muted-foreground">{rate(p.previousFirstBaskets,p.previousGamesTracked)} prior</div></div></div>}

function ProjectedTeam({team,rows}:{team:string;rows:HistoryRow[]}){return <div><div className="text-[10px] uppercase tracking-wide text-muted-foreground mb-2">{team} projected</div>{rows.length?rows.map(r=><div key={`${team}-${r.playerName}`} className="flex justify-between gap-3 py-1.5 text-xs border-b last:border-0"><span className="font-medium">{r.playerName}</span><span className="text-muted-foreground font-mono">{r.fbScored}/{r.gamesTracked} FB</span></div>):<div className="text-xs text-muted-foreground py-3">Not enough verified starter history yet.</div>}</div>}

function GameCard({game,history}:{game:Game;history:HistoryRow[]}){const confirmed=game.lineupStatus==='confirmed';const away=projectedFor(game.awayTeam,history),home=projectedFor(game.homeTeam,history);return <article className="rounded-md border bg-card overflow-hidden"><div className="px-4 py-3 border-b flex flex-wrap items-center justify-between gap-2 bg-muted/20"><div><div className="font-bold text-sm">{game.awayTeam} @ {game.homeTeam}</div><div className="text-[10px] text-muted-foreground flex items-center gap-1"><Clock className="w-3 h-3"/>{time(game.date)} · {game.status}</div></div><Badge className={confirmed?'bg-emerald-500/15 text-emerald-500 border-emerald-500/30':'bg-yellow-500/15 text-yellow-600 border-yellow-500/30'}>{confirmed?'10 STARTERS CONFIRMED':'PROJECTED · NOT LOCKED'}</Badge></div>{!confirmed?<div className="p-4 space-y-4"><div className="rounded-md border bg-yellow-500/5 p-3"><div className="flex gap-2"><AlertCircle className="w-4 h-4 text-yellow-500 mt-0.5"/><div><p className="font-semibold text-xs">Early lineup view</p><p className="text-[10px] text-muted-foreground mt-1">These are projections from this season’s verified starter history. They can change with injuries, trades, rest or game-day decisions. They are never written to the official prediction ledger.</p></div></div></div><div className="grid grid-cols-2 gap-5"><ProjectedTeam team={game.awayTeam} rows={away}/><ProjectedTeam team={game.homeTeam} rows={home}/></div><div className="rounded-md border p-3 flex items-start gap-2"><CircleDot className="w-4 h-4 text-primary mt-0.5"/><div><div className="text-xs font-semibold">Opening tip matchup</div><div className="text-[10px] text-muted-foreground mt-1">Waiting for verified starters/jumpers. We will not guess a center or publish a tip percentage until the matchup can be identified reliably.</div></div></div></div>:<div className="p-4"><div className="rounded-md border p-3 mb-4 flex items-start gap-2"><CircleDot className="w-4 h-4 text-primary mt-0.5"/><div><div className="text-xs font-semibold">Opening tip signal</div><div className="text-[10px] text-muted-foreground mt-1">Confirmed starters are available. Tip probability will only be added once verified jump-ball history is sufficient; it will be a supporting feature, not the whole First Basket model.</div></div></div>{game.topPick&&<div className="rounded-md border bg-primary/5 p-3 mb-4 flex items-center justify-between gap-3"><div><div className="text-[10px] uppercase tracking-wide text-muted-foreground">Top First Basket candidate</div><div className="font-bold">{game.topPick.name} · {game.topPick.team}</div></div><div className="text-2xl font-bold font-mono">{game.topPick.probability.toFixed(1)}%</div></div>}<div>{game.candidates.map(p=><CandidateRow key={`${game.id}-${p.team}-${p.name}`} p={p}/>)}</div></div>}</article>}

function HistoryTable({rows,season}:{rows:HistoryRow[];season:number}){return <div className="rounded-md border bg-card overflow-hidden"><div className="px-4 py-3 border-b font-semibold text-sm">{season} verified First Basket history</div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-muted/20 text-[10px] uppercase text-muted-foreground"><tr><th className="text-left px-4 py-2">Player</th><th className="text-left px-3 py-2">Team</th><th className="text-right px-3 py-2">FB</th><th className="text-right px-3 py-2">Starter games</th><th className="text-right px-4 py-2">Rate</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={`${season}-${r.team}-${r.playerName}`} className="border-t"><td className="px-4 py-2 font-medium">{r.playerName}</td><td className="px-3 py-2 text-muted-foreground">{r.team}</td><td className="px-3 py-2 text-right font-mono">{r.fbScored}</td><td className="px-3 py-2 text-right font-mono">{r.gamesTracked}</td><td className="px-4 py-2 text-right font-mono font-semibold">{r.rate===null?'—':`${r.rate.toFixed(1)}%`}</td></tr>):<tr><td colSpan={5} className="px-4 py-10 text-center text-muted-foreground">No verified history yet.</td></tr>}</tbody></table></div></div>}

export default function WNBA(){const[section,setSection]=useState<'games'|'history'>('games');const slate=useQuery<Slate>({queryKey:['/api/wnba/first-basket'],staleTime:60000,refetchInterval:120000});const history=useQuery<HistoryPayload>({queryKey:['/api/wnba/history'],staleTime:120000,refetchInterval:300000});if(slate.isLoading)return <div className="space-y-4"><Skeleton className="h-9 w-72"/><Skeleton className="h-28"/><Skeleton className="h-80"/></div>;const confirmed=slate.data?.games.filter(g=>g.lineupStatus==='confirmed').length??0;return <div className="-mx-4 md:-mx-6 lg:-mx-8 -mt-8"><div className="border-b bg-card"><div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8 flex items-center justify-between"><div className="flex"><button onClick={()=>setSection('games')} className={`px-4 py-4 text-xs border-b-2 ${section==='games'?'border-primary':'border-transparent text-muted-foreground'}`}>WNBA Games</button><button onClick={()=>setSection('history')} className={`px-4 py-4 text-xs border-b-2 flex gap-1.5 ${section==='history'?'border-primary':'border-transparent text-muted-foreground'}`}><History className="w-3.5 h-3.5"/>FB History</button></div><Button variant="outline" size="sm" onClick={()=>{slate.refetch();history.refetch()}} className="gap-2"><RefreshCw className={`w-3.5 h-3.5 ${(slate.isFetching||history.isFetching)?'animate-spin':''}`}/>Refresh</Button></div></div><div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8 py-6"><div className="mb-5"><h1 className="text-xl font-bold">WNBA First Basket</h1><p className="text-xs text-muted-foreground mt-1">Projected early · confirmed before lock · verified season history</p></div>{section==='games'?<><div className="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6"><div className="rounded-md border bg-card p-4"><Users className="w-4 h-4 text-primary mb-2"/><div className="text-2xl font-bold">{slate.data?.teams.length??0}</div><div className="text-xs text-muted-foreground">teams tracked</div></div><div className="rounded-md border bg-card p-4"><ShieldCheck className="w-4 h-4 text-primary mb-2"/><div className="text-2xl font-bold">{confirmed}/{slate.data?.games.length??0}</div><div className="text-xs text-muted-foreground">confirmed 5+5</div></div><div className="rounded-md border bg-card p-4"><Trophy className="w-4 h-4 text-primary mb-2"/><div className="text-lg font-bold">{slate.data?.modelVersion??'WNBA-FB-SEASONAL-V1'}</div><div className="text-xs text-muted-foreground">WNBA model</div></div></div><div className="grid grid-cols-1 xl:grid-cols-2 gap-4">{slate.data?.games.map(g=><GameCard key={g.id} game={g} history={history.data?.current??[]}/>)}</div></>:history.isLoading?<Skeleton className="h-96"/>:<div className="grid grid-cols-1 xl:grid-cols-2 gap-4"><HistoryTable rows={history.data?.current??[]} season={history.data?.currentSeason??new Date().getUTCFullYear()}/><HistoryTable rows={history.data?.previous??[]} season={history.data?.previousSeason??new Date().getUTCFullYear()-1}/></div>}<div className="mt-6 text-[10px] text-muted-foreground">Updated {slate.data?time(slate.data.updatedAt):'—'} · {slate.data?.source}</div><div className="mt-2 text-[10px] text-muted-foreground">Projected lineups are informational only. Official First Basket predictions lock only from verified 10-player starting lineups. Tip data will be incorporated only when its source and sample are trustworthy.</div></div></div>}
