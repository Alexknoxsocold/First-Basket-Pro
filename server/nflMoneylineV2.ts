import type { ModelGameInput, ModelResult } from './nflModels.js';

export type NflMoneylineV2Result = ModelResult & { fairAmericanOdds:number; consensusProbability:number|null; marketDeltaPoints:number|null; uncertaintyPoints:number; shadow:true; version:'v2-shadow' };
export type NflMoneylineV2Pair={away:NflMoneylineV2Result|null;home:NflMoneylineV2Result|null};
type TeamProfile={ppg:number|null;papg:number|null;ypg:number|null};
type Availability={qbPenalty:number;injuryPenalty:number;qbNote:string|null;injuryCount:number};
type Advanced={epaPerPlay:number|null;passEpaPerPlay:number|null;rushEpaPerPlay:number|null;cpoe:number|null;firstDownRate:number|null;sourceSeason:number|null};
const ESPN='https://site.api.espn.com/apis/site/v2/sports/football/nfl',CACHE_MS=6*60*60*1000;
const profileCache=new Map<string,{expires:number;value:TeamProfile}>(),availabilityCache=new Map<string,{expires:number;value:Availability}>();
let advancedCache:{expires:number;season:number;teams:Map<string,Advanced>}|null=null;
function clamp(v:number,min:number,max:number){return Math.max(min,Math.min(max,v));}function norm(v:string){return v.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]/g,'');}function logit(p:number){const x=clamp(p,.01,.99);return Math.log(x/(1-x));}function sigmoid(x:number){return 1/(1+Math.exp(-x));}function americanProfit(o:number){return o>0?o/100:100/Math.abs(o);}function americanImpliedPct(o:number){return(o>0?100/(o+100):Math.abs(o)/(Math.abs(o)+100))*100;}function ev(p:number,o:number){p/=100;return p*americanProfit(o)-(1-p);}function fairAmerican(x:number){const p=clamp(x/100,.001,.999);return Math.round(p>=.5?-100*p/(1-p):100*(1-p)/p);}function parseRecord(r:string|null){if(!r)return null;const m=r.match(/(\d+)\s*-\s*(\d+)(?:\s*-\s*(\d+))?/);if(!m)return null;const w=+m[1],l=+m[2],t=+(m[3]??0),g=w+l+t;return g?(w+.5*t)/g:null;}
function collectStats(node:unknown,out:Map<string,number>){if(!node||typeof node!=='object')return;if(Array.isArray(node)){node.forEach(x=>collectStats(x,out));return;}const o=node as Record<string,unknown>,name=typeof o.name==='string'?o.name:typeof o.abbreviation==='string'?o.abbreviation:null,value=typeof o.value==='number'?o.value:typeof o.value==='string'&&Number.isFinite(Number(o.value))?Number(o.value):null;if(name&&value!==null)out.set(norm(name),value);Object.values(o).forEach(v=>collectStats(v,out));}function stat(m:Map<string,number>,...names:string[]){for(const n of names){const v=m.get(norm(n));if(v!==undefined&&Number.isFinite(v))return v;}return null;}async function json(url:string,timeout=5000){const c=new AbortController(),timer=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{signal:c.signal,headers:{'User-Agent':'PreziTools/1.0'}});if(!r.ok)throw new Error(String(r.status));return await r.json();}finally{clearTimeout(timer);}}
async function teamProfile(team:string):Promise<TeamProfile>{const key=team.toUpperCase(),hit=profileCache.get(key);if(hit&&hit.expires>Date.now())return hit.value;const season=new Date().getUTCFullYear()-1;for(const url of [`${ESPN}/teams/${encodeURIComponent(team)}/statistics?season=${season}`,`${ESPN}/teams/${encodeURIComponent(team)}/statistics`])try{const payload=await json(url),m=new Map<string,number>();collectStats(payload,m);const value={ppg:stat(m,'pointsPerGame','avgPoints','points'),papg:stat(m,'pointsAllowedPerGame','avgPointsAllowed','pointsAgainst'),ypg:stat(m,'yardsPerGame','totalYardsPerGame')};if(Object.values(value).some(v=>v!==null)){profileCache.set(key,{expires:Date.now()+CACHE_MS,value});return value;}}catch{}const value={ppg:null,papg:null,ypg:null};profileCache.set(key,{expires:Date.now()+3600000,value});return value;}
function walk(node:any,fn:(o:any)=>void){if(!node)return;if(Array.isArray(node)){node.forEach(x=>walk(x,fn));return;}if(typeof node!=='object')return;fn(node);Object.values(node).forEach(x=>walk(x,fn));}
async function availability(team:string):Promise<Availability>{const key=team.toUpperCase(),hit=availabilityCache.get(key);if(hit&&hit.expires>Date.now())return hit.value;let value:Availability={qbPenalty:0,injuryPenalty:0,qbNote:null,injuryCount:0};try{const payload=await json(`${ESPN}/teams/${encodeURIComponent(team)}/injuries`,4500);let skill=0,critical=0;walk(payload,(o:any)=>{const athlete=o.athlete??o,pos=String(athlete?.position?.abbreviation??o.position?.abbreviation??'').toUpperCase(),name=String(athlete?.displayName??athlete?.fullName??o.displayName??''),status=String(o.status??o.type?.description??o.type?.name??'').toLowerCase();if(!name||!status)return;const out=/out|injured reserve|ir\b/.test(status),doubt=/doubtful/.test(status),question=/questionable/.test(status);if(!(out||doubt||question))return;value.injuryCount++;const severity=out?1:doubt?.7:.25;if(pos==='QB'){value.qbPenalty=Math.max(value.qbPenalty,severity*(out?.75:.45));value.qbNote=`${name} ${status}`;}else if(['WR','RB','TE','OT','T','G','C','DE','DT','LB','CB','S'].includes(pos)){skill+=severity;if(out||doubt)critical++;}});value.injuryPenalty=clamp(skill*.018+critical*.012,0,.16);}catch{}availabilityCache.set(key,{expires:Date.now()+1800000,value});return value;}
function csvLine(line:string){const out:string[]=[];let cur='',q=false;for(let i=0;i<line.length;i++){const ch=line[i];if(ch==='"'){if(q&&line[i+1]==='"'){cur+='"';i++;}else q=!q;}else if(ch===','&&!q){out.push(cur);cur='';}else cur+=ch;}out.push(cur);return out;}function n(v:string|undefined){const x=Number(v);return Number.isFinite(x)?x:null;}function canonicalTeam(t:string){const x=t.toUpperCase();return({JAC:'JAX',WSH:'WAS',LA:'LAR'} as Record<string,string>)[x]??x;}
async function advancedTeams():Promise<Map<string,Advanced>>{if(advancedCache&&advancedCache.expires>Date.now())return advancedCache.teams;const season=new Date().getUTCFullYear()-1,url=`https://github.com/nflverse/nflverse-data/releases/download/stats_team/stats_team_reg_${season}.csv`,teams=new Map<string,Advanced>();try{const c=new AbortController(),timer=setTimeout(()=>c.abort(),7000);let text='';try{const r=await fetch(url,{signal:c.signal,headers:{'User-Agent':'PreziTools/1.0','Accept':'text/csv'}});if(!r.ok)throw new Error(`HTTP ${r.status}`);text=await r.text();}finally{clearTimeout(timer);}const lines=text.trim().split(/\r?\n/),headers=csvLine(lines.shift()??'');for(const line of lines){const vals=csvLine(line),row=Object.fromEntries(headers.map((h,i)=>[h,vals[i]])),team=canonicalTeam(String(row.team??''));if(!team)continue;const pass=n(row.passing_epa),rush=n(row.rushing_epa),att=n(row.attempts),carries=n(row.carries),sacks=n(row.sacks_suffered)??0,passFD=n(row.passing_first_downs),rushFD=n(row.rushing_first_downs),plays=(att??0)+(carries??0)+sacks;teams.set(team,{epaPerPlay:plays>0&&pass!==null&&rush!==null?(pass+rush)/plays:null,passEpaPerPlay:(att??0)+sacks>0&&pass!==null?pass/((att??0)+sacks):null,rushEpaPerPlay:(carries??0)>0&&rush!==null?rush/(carries??1):null,cpoe:n(row.passing_cpoe),firstDownRate:plays>0&&passFD!==null&&rushFD!==null?(passFD+rushFD)/plays:null,sourceSeason:season});}}catch(e){console.warn('[NFL V2] nflverse advanced stats unavailable:',e instanceof Error?e.message:String(e));}advancedCache={expires:Date.now()+(teams.size?CACHE_MS:3600000),season,teams};return teams;}
export async function modelMoneylineV2(game:ModelGameInput,awayOdds:number|null,homeOdds:number|null,awayConsensusNoVigPct:number|null,homeConsensusNoVigPct:number|null):Promise<NflMoneylineV2Pair>{const [ap,hp,aa,ha,adv]=await Promise.all([teamProfile(game.away.abbreviation),teamProfile(game.home.abbreviation),availability(game.away.abbreviation),availability(game.home.abbreviation),advancedTeams()]),ae=adv.get(canonicalTeam(game.away.abbreviation)),he=adv.get(canonicalTeam(game.home.abbreviation)),ar=parseRecord(game.away.record)??.5,hr=parseRecord(game.home.record)??.5;let rating=(hr-ar)*1.45+.08;if(hp.ppg!==null&&ap.ppg!==null)rating+=(hp.ppg-ap.ppg)*.025;if(hp.papg!==null&&ap.papg!==null)rating+=(ap.papg-hp.papg)*.024;if(hp.ypg!==null&&ap.ypg!==null)rating+=(hp.ypg-ap.ypg)*.00055;if(ae?.epaPerPlay!=null&&he?.epaPerPlay!=null)rating+=(he.epaPerPlay-ae.epaPerPlay)*1.15;if(ae?.passEpaPerPlay!=null&&he?.passEpaPerPlay!=null)rating+=(he.passEpaPerPlay-ae.passEpaPerPlay)*.65;if(ae?.firstDownRate!=null&&he?.firstDownRate!=null)rating+=(he.firstDownRate-ae.firstDownRate)*.8;if(ae?.cpoe!=null&&he?.cpoe!=null)rating+=(he.cpoe-ae.cpoe)*.012;rating+=(aa.qbPenalty-ha.qbPenalty)+(aa.injuryPenalty-ha.injuryPenalty);const fh=clamp(sigmoid(rating)*100,24,76),fa=100-fh,hasBase=Object.values(ap).some(v=>v!==null)||Object.values(hp).some(v=>v!==null),hasAdvanced=!!(ae&&he&&ae.epaPerPlay!==null&&he.epaPerPlay!==null),hasAvailability=aa.injuryCount>0||ha.injuryCount>0;const build=(footballPct:number,odds:number|null,consensus:number|null,own:Availability,opp:Availability):NflMoneylineV2Result|null=>{if(odds===null||!Number.isFinite(odds))return null;const implied=americanImpliedPct(odds),marketPct=consensus!==null&&Number.isFinite(consensus)?clamp(consensus,1,99):implied,marketWeight=hasAdvanced?.62:hasBase?.68:.76,blended=sigmoid(marketWeight*logit(marketPct/100)+(1-marketWeight)*logit(footballPct/100))*100,disagreement=Math.abs(footballPct-marketPct),uncertainty=clamp((hasAdvanced?.75:hasBase?.9:1.5)+disagreement*.12+((own.qbPenalty>0||opp.qbPenalty>0)?.35:0),.75,5),adjusted=clamp(blended+(marketPct-blended)*(uncertainty/20),5,95),edge=adjusted-implied,expected=ev(adjusted,odds),delta=adjusted-marketPct,confidence=edge>=6&&expected>=.08&&uncertainty<=2.5?'elite':edge>=3&&expected>=.04&&uncertainty<=3.5?'strong':'watch',qualifies=edge>=3&&expected>=.04&&uncertainty<=3.5,reasons=['sportsbook consensus used as market prior','previous-season win-strength baseline','home-field adjustment','uncertainty penalty for model/market disagreement'];if(hasBase)reasons.splice(2,0,'scoring, defensive scoring, and yardage efficiency signal');if(hasAdvanced)reasons.splice(2,0,`nflverse ${ae?.sourceSeason} offensive EPA/play, pass EPA/play, CPOE and first-down-rate signal`);if(hasAvailability)reasons.splice(reasons.length-1,0,'QB availability and injury-impact adjustment');if(own.qbNote)reasons.push(`own QB flag: ${own.qbNote}`);if(opp.qbNote)reasons.push(`opponent QB flag: ${opp.qbNote}`);return{modelProbability:+adjusted.toFixed(1),edgePoints:+edge.toFixed(1),expectedValue:+expected.toFixed(3),confidence,qualifies,reasons,fairAmericanOdds:fairAmerican(adjusted),consensusProbability:consensus===null?null:+marketPct.toFixed(1),marketDeltaPoints:consensus===null?null:+delta.toFixed(1),uncertaintyPoints:+uncertainty.toFixed(1),shadow:true,version:'v2-shadow'};};return{away:build(fa,awayOdds,awayConsensusNoVigPct,aa,ha),home:build(fh,homeOdds,homeConsensusNoVigPct,ha,aa)};}
